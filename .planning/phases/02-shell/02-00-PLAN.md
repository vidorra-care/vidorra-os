---
phase: 02-shell
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shell/vitest.config.ts
  - packages/shell/package.json
  - packages/shell/src/stores/useWindowStore.test.ts
  - packages/shell/src/components/Dock/Dock.test.tsx
  - packages/shell/src/components/Menubar/Menubar.test.tsx
  - packages/shell/src/components/Desktop/Desktop.test.tsx
  - packages/shell/src/App.test.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - SHELL-06
  - SHELL-07
  - SHELL-08

must_haves:
  truths:
    - "Vitest runs in jsdom environment for React component tests"
    - "All 5 test stub files exist and compile without errors"
    - "Running pnpm test --filter @vidorra/shell exits green (all stubs are .todo or .skip)"
  artifacts:
    - path: "packages/shell/vitest.config.ts"
      provides: "Shell-specific Vitest config with jsdom environment"
      contains: "environment"
    - path: "packages/shell/src/stores/useWindowStore.test.ts"
      provides: "Test stubs for SHELL-01, SHELL-02, SHELL-03"
    - path: "packages/shell/src/components/Dock/Dock.test.tsx"
      provides: "Test stubs for SHELL-04, SHELL-05"
    - path: "packages/shell/src/components/Menubar/Menubar.test.tsx"
      provides: "Test stubs for SHELL-06"
    - path: "packages/shell/src/components/Desktop/Desktop.test.tsx"
      provides: "Test stubs for SHELL-07"
    - path: "packages/shell/src/App.test.tsx"
      provides: "Test stubs for SHELL-08"
  key_links:
    - from: "packages/shell/vitest.config.ts"
      to: "packages/shell/package.json"
      via: "vitest devDependency enables test execution"
      pattern: "vitest"
---

<objective>
Create the test infrastructure for the shell package: vitest config with jsdom environment, testing-library devDependencies, and stub test files for all 5 test targets. Stubs use `it.todo` or `describe.skip` so they compile green but do not fail. This is Wave 0 — all subsequent plans depend on this infrastructure existing.

Purpose: VALIDATION.md identifies 5 missing test files and a missing vitest config as Wave 0 gaps. This plan closes all of them so that plans 02-01 through 02-05 can run their behavioral tests against real stubs.
Output: vitest.config.ts, 5 test stub files, devDependencies installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell/02-CONTEXT.md
@.planning/phases/02-shell/02-RESEARCH.md
@.planning/phases/02-shell/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test devDependencies and create vitest.config.ts</name>
  <files>
    packages/shell/package.json
    packages/shell/vitest.config.ts
  </files>
  <read_first>
    packages/shell/package.json
    packages/shell/tsconfig.json
    vitest.config.ts
    packages/kernel/vitest.config.ts
  </read_first>
  <action>
1. Install test devDependencies for the shell package:
   ```bash
   cd packages/shell && pnpm add -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @types/react @types/react-dom
   ```

2. Create `packages/shell/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'

   export default defineConfig({
     plugins: [react()],
     test: {
       globals: true,
       environment: 'jsdom',
       setupFiles: [],
       css: { modules: { classNameStrategy: 'non-scoped' } },
     },
     resolve: {
       conditions: ['development', 'browser'],
     },
   })
   ```

3. Add test scripts to `packages/shell/package.json` if not already present:
   ```json
   "scripts": {
     "test": "vitest run",
     "test:watch": "vitest"
   }
   ```

4. Ensure `packages/shell/tsconfig.json` includes `"types": ["vitest/globals"]` in compilerOptions so test globals (`describe`, `it`, `expect`) are recognized without imports.
  </action>
  <verify>
    <automated>cd D:/Code/Study/vidorra-os && pnpm test --filter @vidorra/shell -- --run 2>&1 | head -20</automated>
  </verify>
  <done>vitest configured with jsdom environment; testing-library installed; test command runs without config errors</done>
</task>

<task type="auto">
  <name>Task 2: Create all 5 test stub files with it.todo placeholders</name>
  <files>
    packages/shell/src/stores/useWindowStore.test.ts
    packages/shell/src/components/Dock/Dock.test.tsx
    packages/shell/src/components/Menubar/Menubar.test.tsx
    packages/shell/src/components/Desktop/Desktop.test.tsx
    packages/shell/src/App.test.tsx
  </files>
  <action>
1. Create `packages/shell/src/stores/useWindowStore.test.ts`:
   ```typescript
   describe('useWindowStore', () => {
     describe('SHELL-01: multi-window drag/resize state', () => {
       it.todo('openWindow adds a window with staircase offset')
       it.todo('openWindow assigns incrementing zIndex')
       it.todo('setWindowRect updates position and size')
       it.todo('multiple windows exist simultaneously with independent state')
     })

     describe('SHELL-02: minimize/maximize/close transitions', () => {
       it.todo('closeWindow removes the window by id')
       it.todo('setWindowState changes window state to minimized')
       it.todo('setWindowState(minimized) sets focused=false')
       it.todo('toggleMaximize toggles between normal and maximized')
       it.todo('restoring from maximized uses saved preMaximizeRect')
     })

     describe('SHELL-03: z-index focus management', () => {
       it.todo('focusWindow sets target focused=true with highest zIndex')
       it.todo('focusWindow sets all others focused=false')
     })
   })
   ```

2. Create `packages/shell/src/components/Dock/Dock.test.tsx`:
   ```typescript
   describe('Dock', () => {
     describe('SHELL-04: running indicator', () => {
       it.todo('shows running dot when a window is open for an appId')
       it.todo('hides running dot when no window is open for an appId')
     })

     describe('SHELL-05: right-click context menu', () => {
       it.todo('right-clicking a running app shows 3 menu items: 打开, 在 Dock 中隐藏, 关闭')
       it.todo('right-clicking a non-running app shows 1 menu item: 打开')
     })
   })
   ```

3. Create `packages/shell/src/components/Menubar/Menubar.test.tsx`:
   ```typescript
   describe('Menubar', () => {
     describe('SHELL-06: app name and clock', () => {
       it.todo('shows focused app name when a window is focused')
       it.todo('shows Vidorra OS when no window is focused')
       it.todo('clock displays HH:mm format')
     })
   })
   ```

4. Create `packages/shell/src/components/Desktop/Desktop.test.tsx`:
   ```typescript
   describe('Desktop', () => {
     describe('SHELL-07: wallpaper', () => {
       it.todo('renders wallpaper from localStorage vidorra:wallpaper')
       it.todo('falls back to default.png when localStorage is empty')
       it.todo('right-click shows context menu with 4 entries')
     })
   })
   ```

5. Create `packages/shell/src/App.test.tsx`:
   ```typescript
   describe('App', () => {
     describe('SHELL-08: Welcome window', () => {
       it.todo('opens Welcome window on first launch when vidorra:welcomed is absent')
       it.todo('does NOT open Welcome window when vidorra:welcomed is already set')
     })
   })
   ```

All stubs use `it.todo(...)` which is recognized by vitest as pending tests that do not fail. This ensures `pnpm test --filter @vidorra/shell` exits green.
  </action>
  <verify>
    <automated>cd D:/Code/Study/vidorra-os && pnpm test --filter @vidorra/shell -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test stub files exist with it.todo placeholders covering SHELL-01 through SHELL-08. Running pnpm test exits green with all tests shown as pending/todo.</done>
</task>

</tasks>

<verification>
- `pnpm test --filter @vidorra/shell -- --run` exits with code 0
- All 5 stub files exist and are recognized by vitest
- `packages/shell/vitest.config.ts` contains `environment: 'jsdom'`
- No test failures (all stubs are `it.todo`)
</verification>

<success_criteria>
- Test infrastructure ready for all downstream plans
- Every test file listed in VALIDATION.md Wave 0 Requirements exists
- Shell vitest environment configured with jsdom for React component testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell/02-00-SUMMARY.md`
</output>
